# Agent bundle ships via GitHub only - not deployed to Vercel CDN
# (too large for Vercel static, users pull from GitHub directly)
agent-bundle/

# Archives ship via GitHub only - not deployed to Vercel
*.zip

# Desktop app source — kept in-repo as the canonical parity reference only,
# never deployed or built by the web app.
desktop-reference/
